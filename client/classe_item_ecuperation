# Création d'une instance de ItemLoader
item_loader = ItemLoader('path/to/weapons.json', 'path/to/armor.json')

# Récupération d'une arme par son ID
weapon = item_loader.get_weapon(1)
print(weapon)

# Récupération d'une armure par son ID
armor = item_loader.get_armor(1)
print(armor)
